// Copy this file to Config/Secrets.xcconfig and fill local values.
// In xcconfig files, `//` starts a comment, so relay URLs must use `/$()/`
// to preserve the double slash after the scheme.

// Direct mode is deprecated; relay-only is supported.
// AI_BACKEND_MODE = direct
// GEMINI_API_KEY = leave unset; not shipped in builds
// GEMINI_MODEL = gemini-3-flash-preview
// GEMINI_TRANSCRIPTION_MODEL = gemini-3-flash-preview

// Relay mode example
AI_BACKEND_MODE = relay
AI_RELAY_BASE_URL = http:/$()/127.0.0.1:8787
// Production builds MUST leave AI_RELAY_BEARER_TOKEN unset.
// The watch obtains a per-device rk_… key via /v1/activation/bootstrap
// or StoreKit purchase. Setting AI_RELAY_BEARER_TOKEN here is for
// DEBUG-only local development against a relay you control.
// AI_RELAY_BEARER_TOKEN = dev-only-token
// Only enable this when your relay tunnel uses a self-signed or otherwise invalid HTTPS certificate.
// AI_RELAY_ALLOW_INSECURE_TLS = YES
GEMINI_MODEL = gemini-3-flash-preview
GEMINI_TRANSCRIPTION_MODEL = gemini-3-flash-preview

// Offline activation signing secret. MUST be set; build will fail without it.
// Set the same value in your relay's environment so the server can issue
// matching codes.
OFFLINE_ACTIVATION_SIGNING_SEED =

// Optional shared storage container for future iPhone/watch sync.
// APP_GROUP_IDENTIFIER = group.your.company.aichat

// Shared iCloud ubiquity container for cross-device conversation sync.
// ICLOUD_CONTAINER_IDENTIFIER = iCloud.hanbin.AIChat
